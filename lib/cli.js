// Generated by CoffeeScript 2.5.1
(function() {
  var Client, Server, YAML, build, cwd, ensureDirExists, fs, getSources, path, printHelp;

  fs = require('fs');

  path = require('path');

  YAML = require('yaml');

  require('./ext');

  ({Client} = require('./client'));

  ({Server} = require('./server'));

  ({ensureDirExists} = require('./util'));

  cwd = process.cwd();

  exports.run = function() {
    var _agn, _node, command;
    [_node, _agn, command] = process.argv;
    switch (command) {
      case 'build':
        return build();
      default:
        return printHelp();
    }
  };

  build = function() {
    var dir, sources;
    dir = `${cwd}/build`;
    ensureDirExists(dir);
    sources = getSources();
    Server(sources)(`${dir}/server`);
    return Client(sources)(`${dir}/client`);
  };

  getSources = function() {
    var api, functions, i, key, len, name, ref;
    name = path.basename(cwd);
    api = YAML.parse(fs.readFileSync(`${cwd}/api.yml`, 'utf-8'));
    functions = {};
    ref = Object.keys(api.functions);
    for (i = 0, len = ref.length; i < len; i++) {
      key = ref[i];
      functions[key] = fs.readFileSync(`${cwd}/functions/${key}.coffee`, 'utf-8');
    }
    return {name, api, functions};
  };

  printHelp = function() {
    return console.log('printHelp');
  };

}).call(this);
