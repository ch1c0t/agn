// Generated by CoffeeScript 2.5.1
(function() {
  var Creator, auth, createFnFiles, createMainFile, createPackageFile, parseEntities;

  ({Creator} = require('./creator'));

  ({createPackageFile} = require('./server/package'));

  ({createMainFile} = require('./server/main'));

  ({parseEntities, createFnFiles} = require('./server/functions'));

  ({auth} = require('./server/auth'));

  exports.Server = Creator({
    init: {
      name: function() {
        return this;
      },
      api: function() {
        return this;
      },
      functions: parseEntities,
      auth: auth
    },
    once: function() {
      return this.embed([createPackageFile, createMainFile, createFnFiles]);
    },
    call: function(dir) {
      this.inside(dir, function() {
        this.createPackageFile();
        this.createMainFile();
        return this.createFnFiles();
      });
      return console.log(`Building the server at ${dir}.`);
    }
  });

}).call(this);
