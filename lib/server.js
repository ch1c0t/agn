// Generated by CoffeeScript 2.5.1
(function() {
  var Fns, Generator, createFnFiles, createMainFile, createPackageFile;

  ({Generator} = require('./generator'));

  ({createPackageFile} = require('./server/package'));

  ({createMainFile} = require('./server/main'));

  ({Fns, createFnFiles} = require('./server/functions'));

  exports.Server = Generator({
    init: {
      name: function() {
        return this;
      },
      api: function() {
        return this;
      },
      functions: Fns
    },
    once: function() {
      createPackageFile.call(this);
      createMainFile.call(this);
      return createFnFiles.call(this);
    },
    call: function(dir) {
      this.inside(dir, function() {
        this.createPackageFile();
        this.createMainFile();
        return this.createFnFiles();
      });
      return console.log(`Building the server at ${dir}.`);
    }
  });

}).call(this);
